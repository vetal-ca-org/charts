name: Helm Chart CI

on:
  push:
    branches:
      - master
      - "feature/**"
    paths:
      - "charts/**"
      - ".github/workflows/helm.yaml"
  pull_request:
    branches:
      - master
      - "feature/**"
    paths:
      - "charts/**"
      - ".github/workflows/helm.yaml"

jobs:
  lint-and-package:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        chart:
          - zerotier
          - openconnect
          - cert-manager
          - syncthing
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Helm
        uses: azure/setup-helm@v4
        with:
          version: v3.14.4

      - name: Helm lint
        run: helm lint charts/${{ matrix.chart }}

      - name: Helm dependency build
        run: helm dependency build charts/${{ matrix.chart }}

      - name: Derive package metadata
        id: meta
        shell: bash
        run: |
          BRANCH="${GITHUB_HEAD_REF:-${GITHUB_REF_NAME}}"
          CHART_DIR="charts/${{ matrix.chart }}"
          BASE_VERSION=$(awk '/^version:/ {print $2}' "${CHART_DIR}/Chart.yaml")
          if [[ "${BRANCH}" == "master" ]]; then
            PACKAGE_VERSION="${BASE_VERSION}"
            ARTIFACT_SUFFIX="release"
          else
            SANITIZED=$(echo "${BRANCH}" | tr '[:upper:]' '[:lower:]' | sed -E 's/[^a-z0-9]+/-/g' | sed -E 's/^-+|-+$//g')
            PACKAGE_VERSION="${BASE_VERSION}-${SANITIZED}"
            ARTIFACT_SUFFIX="${SANITIZED}"
          fi
          echo "branch=${BRANCH}" >> "${GITHUB_OUTPUT}"
          echo "version=${PACKAGE_VERSION}" >> "${GITHUB_OUTPUT}"
          echo "artifact_suffix=${ARTIFACT_SUFFIX}" >> "${GITHUB_OUTPUT}"

      - name: Helm package
        run: |
          mkdir -p dist
          helm package "charts/${{ matrix.chart }}" --destination dist --version "${{ steps.meta.outputs.version }}" --app-version "${{ steps.meta.outputs.version }}"

      - name: Upload packaged chart
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.chart }}-chart-${{ steps.meta.outputs.artifact_suffix }}
          path: dist/*.tgz

